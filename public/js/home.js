function initialize_home() {
	// $('div#content').load('/api/home/', function(responseText, textStatus, jqXHR) {
	// 	if (textStatus === "error") {
	// 		$("#load-error").show();
	// 	}
	// });
	clear_filters();
}